<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notepad">
        <activity
            android:name=".category_note.CategoryNoteAdapter"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".category_note.CategoryNoteActivity"
            android:exported="false" />
        <activity
            android:name=".category.NoteWithCategoryActivity"
            android:exported="false" />
        <activity
            android:name=".category.AddCategoryActivity"
            android:exported="false" />
        <activity
            android:name=".category.CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".note.EditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".note.AddNoteActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>